org.gradle.jvmargs=-Xmx3g
android.useAndroidX=true
kotlin.code.style=official

# The on-device LLM runtime under test. Overwritten by the CI discovery step
# with whatever it actually finds on Google's Maven, so a wrong guess here
# costs a log line rather than a build.
spike.llmCoordinate=com.google.mediapipe:tasks-genai:0.10.24
